[package]
name = "repo"
description = "A tool for repo management."
version = "0.1.2"
edition = "2021"
license = "MIT"

[dependencies]
cargo_metadata = "0.18.1"
clap = { version = "4.5.21", features = ["derive"] }
clap_complete = "4.5.38"
edit = "0.1.5"
opener = { version = "0.7.2", features = ["reveal"] }
